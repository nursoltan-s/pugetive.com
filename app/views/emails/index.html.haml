- title('Emails')

%table
  %thead
    %tr
      %th Address
      %th Category
      %th
      %th
      %th
      
  %tbody
    - @emails.each do |email|
      %tr
        %td= email.address
        %td= email.category
        %td= link_to 'Show', email
        %td= link_to 'Edit', edit_email_path(email)
        %td= link_to 'Destroy', email, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Email', new_email_path, class: 'button'
