.row
  .medium-7.columns.medium-offset-5
    = render "resume/button"

.row
  .small-12.medium-offset-3-medium-6.columns
    = render "layouts/tabs", tabs: {'Featured Projects' => render(@interest.works.favorite.sorted),
                                   'All Professional Projects' => render(@interest.works.pro.sorted),
                                   'Side Projects' => render(@interest.works.solo.sorted)}

