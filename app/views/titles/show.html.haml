- title(@title.name, false)

.row
  .medium-7.medium-offset-5.columns
    %h1
      %span.tool-name= @title.name
    %p
      Listing works where I operated as #{content_tag(:i, @title.name)}.

- if @title.works.size < 6
  = render("works/list", works: @title.works)
- else
  = render 'works/stacked', works: @title.works, show_meta: true, show_genre: true
